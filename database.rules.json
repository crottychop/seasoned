{
  "rules": {
    ".read": false,
    ".write": false,
    "pantryOut": {
      ".read": "auth != null",
      ".write": "auth != null",
      "$slug": {
        ".validate": "newData.isBoolean()"
      }
    }
  }
}
